
BasicGame.MainMenu = function (game) {

  this.music = null;
  this.playButton = null;

};

BasicGame.MainMenu.prototype = {

  create: function () {
    this.stage.backgroundColor = '#ddd';

    //  We've already preloaded our assets, so let's kick right into the Main Menu itself.
    //  Here all we're doing is playing some music and adding a picture and button
    //  Naturally I expect you to do something significantly better :)

    this.add.text(this.game.width / 2, 300, "zero", { font: "150px Roboto Mono", fill: "#222", align: "center"}).anchor.setTo(0.5, 0.5);

    this.add.text(this.game.width / 2, 600, "connect digits so they sum up to zero", { font: "30px Roboto Mono", fill: "#444", align: "center"}).anchor.setTo(0.5, 0.5);
    this.add.text(this.game.width / 2, 650, "every carry increases the selection's score", { font: "30px Roboto Mono", fill: "#444", align: "center"}).anchor.setTo(0.5, 0.5);
    this.add.text(this.game.width / 2, 700, "game ends when time runs out", { font: "30px Roboto Mono", fill: "#444", align: "center"}).anchor.setTo(0.5, 0.5);
    this.add.text(this.game.width / 2, 800, "each selected digit gives 2 extra seconds but", { font: "30px Roboto Mono", fill: "#444", align: "center"}).anchor.setTo(0.5, 0.5);
    this.add.text(this.game.width / 2, 850, "has a chance to create blockers", { font: "30px Roboto Mono", fill: "#444", align: "center"}).anchor.setTo(0.5, 0.5);

    this.add.text(this.game.width / 2, 900, "prime length selections can clear blockers but", { font: "30px Roboto Mono", fill: "#444", align: "center"}).anchor.setTo(0.5, 0.5);
    this.add.text(this.game.width / 2, 950, "do not give extra time", { font: "30px Roboto Mono", fill: "#444", align: "center"}).anchor.setTo(0.5, 0.5);

    this.loadingText = this.add.text(this.game.width / 2, this.game.height / 2 + 430, "tap/click to start", { font: "50px Roboto Mono", fill: "#222" });
    this.loadingText.anchor.setTo(0.5, 0.5);
    this.add.text(this.game.width / 2, this.game.height - 75, "Copyright (c) 2015 Bryan Bibat", { font: "20px Roboto Mono", fill: "#444", align: "center"}).anchor.setTo(0.5, 0.5);
    

  },

  update: function () {

    if (this.input.keyboard.isDown(Phaser.Keyboard.Z) || this.input.activePointer.isDown) {
      this.startGame();
    }
    //  Do some nice funky main menu effect here

  },

  startGame: function (pointer) {

    //  Ok, the Play Button has been clicked or touched, so let's stop the music (otherwise it'll carry on playing)
    // this.music.stop();

    //  And start the actual game
    this.state.start('Game');

  }

};
