
BasicGame.MainMenu = function (game) {

  this.music = null;
  this.playButton = null;

};

BasicGame.MainMenu.prototype = {

  create: function () {
    this.stage.backgroundColor = '#ddd';

    //  We've already preloaded our assets, so let's kick right into the Main Menu itself.
    //  Here all we're doing is playing some music and adding a picture and button
    //  Naturally I expect you to do something significantly better :)

    this.add.text(this.game.width / 2, 280, "zero", { font: "150px Roboto Mono", fill: "#222", align: "center"}).anchor.setTo(0.5, 0.5);

    this.add.text(this.game.width / 2, 560, "connect digits so the sum ends in zero", { font: "30px Roboto Mono", fill: "#444", align: "center"}).anchor.setTo(0.5, 0.5);
    this.add.text(this.game.width / 2, 600, "the higher the sum, the greater the points", { font: "30px Roboto Mono", fill: "#444", align: "center"}).anchor.setTo(0.5, 0.5);
    this.add.text(this.game.width / 2, 640, "tap/click/drag to select or unselect digits", { font: "30px Roboto Mono", fill: "#444", align: "center"}).anchor.setTo(0.5, 0.5);
    this.add.text(this.game.width / 2, 740, "each digit gives two extra seconds but", { font: "30px Roboto Mono", fill: "#444", align: "center"}).anchor.setTo(0.5, 0.5);
    this.add.text(this.game.width / 2, 780, "has a chance to create blockers", { font: "30px Roboto Mono", fill: "#444", align: "center"}).anchor.setTo(0.5, 0.5);

    this.add.text(this.game.width / 2, 820, "digits from prime length chains can clear", { font: "30px Roboto Mono", fill: "#444", align: "center"}).anchor.setTo(0.5, 0.5);
    this.add.text(this.game.width / 2, 860, "blockers but only give a half second each", { font: "30px Roboto Mono", fill: "#444", align: "center"}).anchor.setTo(0.5, 0.5);

    this.add.text(this.game.width / 2, 960, "aim for one million points", { font: "30px Roboto Mono", fill: "#444", align: "center"}).anchor.setTo(0.5, 0.5);

    //this.add.text(this.game.width / 2, 1000, "(hint: spamming alone is inefficient)", { font: "24px Roboto Mono", fill: "#444", align: "center"}).anchor.setTo(0.5, 0.5);

    this.loadingText = this.add.text(this.game.width / 2, this.game.height / 2 + 455, "tap/click to start", { font: "60px Roboto Mono", fill: "#222" });
    this.loadingText.anchor.setTo(0.5, 0.5);
    this.add.text(this.game.width / 2, this.game.height - 50, "Copyright (c) 2015 Bryan Bibat", { font: "20px Roboto Mono", fill: "#444", align: "center"}).anchor.setTo(0.5, 0.5);

  },

  update: function () {

    if (this.input.keyboard.isDown(Phaser.Keyboard.SPACEBAR) || this.input.activePointer.isDown) {
      this.startGame();
    }
    //  Do some nice funky main menu effect here

  },

  startGame: function (pointer) {

    //  Ok, the Play Button has been clicked or touched, so let's stop the music (otherwise it'll carry on playing)
    // this.music.stop();

    //  And start the actual game
    this.state.start('Game');

  }

};
